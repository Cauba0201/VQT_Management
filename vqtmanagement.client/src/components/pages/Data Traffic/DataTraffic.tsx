import { Box } from "@mui/material";
// import { tokens } from "../../../theme";

const DataTraffic = () => {
  // const theme = useTheme();
  // const colors = tokens(theme.palette.mode);
  return (
    <Box>data trafict</Box>
    
  );
};

export default DataTraffic;
